import deviceSizes from "./deviceSizes";

const textInputStyle = {
    padding: 10,
    width: '80%',
    borderWidth: 1,
    borderRadius: 10,
    marginVertical: 10,
    fontSize: deviceSizes.deviceWidth/20,
    color: 'black'
}

export default textInputStyle;